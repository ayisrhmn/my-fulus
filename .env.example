# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Dev only: set to "true" to skip auth guards (magic-link lockout escape hatch)
NEXT_PUBLIC_DISABLE_AUTH=
